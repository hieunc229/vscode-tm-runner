# Start your line with hashtag to commend out. Any line without hashtag will have "▶ Run" button above it

echo "Hello world"

# TM Runner also support multi-line command, by adding `\` to the end of each line (similar to Terminal) 

echo "This should" && \
  echo "be run all together" && \
  echo "and this should as well"
